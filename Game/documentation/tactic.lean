import GameServer.Commands
import Game.Structure
/--
``exact`` closes the goal if it applies to the current goal.
-/
TacticDoc exact

/--
``basic`` closes the goal if the goal is `Prop`.
-/
TacticDoc basic

/--
``intro`` add a assumption to the proof via implication.
-/
TacticDoc intro
